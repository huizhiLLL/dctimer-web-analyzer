# 项目指导

本项目是一个在浏览器本地运行的 DCTimer 数据分析器，重点是导入 `.db` 文件、解析数据并生成可读报告。

## 持久约束

- 优先保持“纯前端、本地处理、无服务端依赖”的方向。
- 修改分析或解析逻辑时，先复用 `src/lib/db` 与 `src/lib/analysis` 的现有结构。
- 路由与页面保持清晰分层：导入、筛选、报告分别独立。
- 文案与说明保持简洁，避免把 README 写成开发日志。
- 新增功能时优先补对应测试，至少覆盖核心解析与统计逻辑。
- 除非明确需要，不引入重量级依赖或复杂状态管理。
